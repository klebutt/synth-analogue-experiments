# Runtime dependencies for the Synth subnet miner and dashboard
# Installed on the server at 167.71.143.194 under python3.11

# Core numerics
numpy>=1.24.0
scipy>=1.10.0

# Market data — used by volatility_calculator.py and dashboard/app.py
yfinance>=0.2.40

# Bittensor — used by dashboard/app.py (/api/chain) and the miner itself
# Version on server: 10.0.1  (note: use bt.Subtensor, not bt.subtensor)
bittensor>=9.0.0

# Dashboard web server
flask>=3.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
